# 📌 버전 기록 (ver.md)

이 파일은 코드 수정 시마다 **버전 · 날짜 · 변경 내용**을 기록합니다.
형식: `## vX.Y.Z (YYYY-MM-DD)` + 요약 + 상세 항목.

---

## v0.1.3 (2026-06-10)
**스케줄을 GitHub Actions cron → 로컬 Mac crontab 으로 전환**

- 6/9~6/10 정기 실행 4건 모두 1시간 18분~3시간 52분 지연, 6/10 12:00분은 미실행(누락) 확인
  → GitHub 무료 cron 의 구조적 한계로 판단, 정시 발송 불가
- 로컬 crontab 에 07:00/12:00/21:00 KST 정각 발송 등록 (`run_brief.sh` 래퍼 신규, 로그 `logs/` 30일 보관)
- 워크플로(`market-brief.yml`)는 schedule 트리거 제거, 수동 실행(workflow_dispatch) 백업 전용으로 유지
- 대상 파일: `run_brief.sh`(신규) · `.github/workflows/market-brief.yml` · `README.md`

## v0.1.2 (2026-06-09)
**지수 표기 순서 통일 + 클라우드 자동발송 활성화**

- 지수 표기를 항상 `등락률%(지수값)` 순서로 강제 (1번 말풍선 헤더 요약 포함). `지수값(등락률%)` 역순 금지 명시
- GitHub Secrets 3개(GEMINI_API_KEY · KAKAO_REST_API_KEY · KAKAO_REFRESH_TOKEN) 등록 → 클라우드 cron 자동발송 활성화
- 대상 파일: `market_brief/summarizer.py`

## v0.1.1 (2026-06-09)
**카톡 메시지 형식 대폭 개선 + 말풍선 3 → 5개 확장**

- 말풍선 구성을 3개 → **5개**로 확장
  1. 헤더 + 핵심 요약
  2. 지수·환율 상세
  3. 주도 섹터·종목
  4. 수급·원인 분석
  5. 종합 판단 + 핵심 인사이트
- **이모지**: 1번 말풍선 헤더 1줄에만 이모지, 그 외 모든 줄은 `- `(하이픈)으로 시작
- **줄바꿈**: 내용·단락이 바뀔 때마다 빈 줄(`\n\n`) 삽입 → 가독성 향상
- **지수 표기**: 지수는 `등락률%(지수값)` 형식, 지수값은 반올림 정수 + 천단위 콤마 (예: `+3.68%(7,760)`). 개별 종목은 등락률(%)만 표기
- **문체**: 모든 문장을 음슴체(`~음/~임/~함`)로 종결, `~습니다/~입니다` 제거
- **길이 규칙**: 5블록 합계 600~750자 목표로 상향(빈 줄 포함 글자수 관리)
- `_clip()` 보강: 단어 중간 절단 방지(빈 줄·문장 종결 경계 우선), 음슴체 종결(`음./임./함.`) 인식
- 대상 파일: `market_brief/summarizer.py`

## v0.1.0 (2026-06-09)
**초기 버전 — GitHub 저장소 `brief_07-12-21` 최초 푸시**

- 네이버 금융 실시간 데이터 수집 → Gemini 요약 → 카카오 '나에게 보내기' 발송
- GitHub Actions cron 자동 발송 (07:00 / 12:00 / 21:00 KST, slot morning/noon/evening)
- 말풍선 3개, 설명형 + 개조식 혼합 문체
